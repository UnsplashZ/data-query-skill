# Historical SQL moved

The original historical SQL files were normalized to ASCII filenames. Use:

- `../../../../old-sql/sql/` for SQL files.
- `../../../../historical-sql-index.md` for trust levels and domain tags.
