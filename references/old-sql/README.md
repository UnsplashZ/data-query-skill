# Normalized Historical SQL

Historical SQL files have been copied into `sql/` with ASCII-only filenames. Use `../historical-sql-index.md` or `historical-sql-index.csv` for business-domain tags, trust levels, and source hints.

Search examples:

```bash
python scripts/search_old_sql.py refund --domain refund --limit 20
```
